ANTHROPIC_API_KEY=your_anthropic_api_key_here

# LLM provider: "ollama" (local, free) or "anthropic" (cloud, requires API key)
LLM_PROVIDER=ollama

# Ollama host (only used when LLM_PROVIDER=ollama)
OLLAMA_HOST=http://localhost:11434
